CREATE DATABASE clientsDB; 
USE clientsDB; 
CREATE TABLE Clients (
    ClientID INT AUTO_INCREMENT PRIMARY KEY,
    FullName VARCHAR(100) NOT NULL,
    Email VARCHAR(100) UNIQUE NOT NULL,
    Password VARCHAR(255) NOT NULL
);
CREATE TABLE Meetings (
    MeetingID INT AUTO_INCREMENT PRIMARY KEY,
    ClientID INT NOT NULL,
    Topic VARCHAR(255) NOT NULL,
    NumberOfPeople INT DEFAULT 1,
    MeetingDate DATETIME NOT NULL,
    FOREIGN KEY (ClientID) REFERENCES Clients(ClientID) ON DELETE CASCADE
);
INSERT INTO Clients (FullName, Email, Password) 
VALUES ('Scooby Doo', 'Scoobydoo@example.com', 'Myst3ryMach1n3!');
INSERT INTO Meetings (ClientID, Topic, NumberOfPeople, MeetingDate) 
VALUES (1, 'Lunch', 6, '2026-03-09 11:00:00');
UPDATE Clients 
SET Email = 'Scoobydoo@gmail.com' 
WHERE ClientID = 1;
DELETE FROM Meetings WHERE MeetingID = 1;
DELETE FROM Clients WHERE ClientID = 1;
INSERT INTO Clients (FullName, Email, Password) 
VALUES 
('Fred Jones', 'f.jones@gmail.com', 'Trapper123!'), 
('Daphne Blake', 'purplevibes@gmail.com', 'Sc3nc3Qu33n!'), 
('Velma Dinkley', 'velma.dinkley@gmail.com','Book1shG1rl'), 
('Shaggy Rogers', 'Scoobysnacker@gmail.com', 'R0g3rThat!!!'), 
('Scooburt Doo', 'Scoobydoo@gmail.com', 'Lunchtime');
INSERT INTO Meetings (ClientID, Topic, NumberOfPeople, MeetingDate) 
VALUES 
(2, 'Solve the Chain Rattling Ghost Case', 5, '2026-03-09 09:00:00'),
(3, 'Interview with the mayor', 5, '2026-03-10 07:00:00'), 
(4, 'Restock the Mystery Machine', 3, '2026-03-11 09:00:00');
SELECT Clients.FullName, Meetings.Topic, Meetings.MeetingDate
FROM Clients
INNER JOIN Meetings ON Clients.ClientID = Meetings.ClientID;
SHOW DATABASES;
INSERT INTO Meetings (ClientID, Topic, NumberOfPeople, MeetingDate) 
VALUES 
(4, 'Set a Trap', 5, '2026-03-09 14:00:00');
